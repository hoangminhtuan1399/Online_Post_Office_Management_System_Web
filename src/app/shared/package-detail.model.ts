export interface PackageDetail {
    id: string;
  senderId: string;
  serviceId: string;
  weight: number;
  deliveryNumber: string;
  descriptionId: string;
  paymentId: string;
  deliveryId: string;
  receiver: string;
  createdAt: Date;
}
